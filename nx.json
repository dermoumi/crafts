{
  "extends": "nx/presets/npm.json",
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "npmScope": "crafts",
  "tasksRunnerOptions": {
    "default": {
      "runner": "nx/tasks-runners/default",
      "options": {
        "cacheableOperations": ["build", "lint", "test", "test:cov"]
      }
    }
  },
  "namedInputs": {
    "sourceFiles": [
      "{projectRoot}/src/**/*",
      "!{projectRoot}/src/**/*.test.ts",
      "{projectRoot}/package.json",
      "{projectRoot}/tsconfig.json",
      "{projectRoot}/vite.config.ts"
    ],
    "testFiles": ["{projectRoot}/src/**/*", "{projectRoot}/setup-tests.ts"],
    "miscFiles": ["{projectRoot}/**/*.md"],
    "lintConfig": [
      "{projectRoot}/.eslintrc.cjs",
      "{projectRoot}/.eslintignore",
      "{projectRoot}/.prettierignore",
      "{projectRoot}/tsconfig.*.json"
    ],
    "baseLintConfig": ["{projectRoot}/.noop"]
  },
  "targetDefaults": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["sourceFiles", "^sourceFiles"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "inputs": [
        "miscFiles",
        "sourceFiles",
        "^sourceFiles",
        "testFiles",
        "lintConfig",
        "^baseLintConfig"
      ],
      "outputs": [".eslintcache"]
    },
    "test": {
      "inputs": ["testFiles", "^sourceFiles"]
    },
    "test:cov": {
      "inputs": ["testFiles", "^sourceFiles"],
      "outputs": ["coverage/**"]
    }
  }
}
