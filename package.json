{
  "name": "@crafts/monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "nx run-many --target=build",
    "dev": "pnpm -r --parallel run dev",
    "lint": "nx run-many --target=lint",
    "test": "nx run-many --target=test",
    "test:cov": "nx run-many --target=test:cov",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "nx": "^15.8.6"
  }
}
